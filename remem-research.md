# AI 工程师智能体的记忆系统：调研分析与设计方案

> 调研日期：2026-05-30
> 调研对象：Devin AI、OpenCLAW、Hermes Agent、Mem0、Claude Code、OpenCode
> 更新日期：2026-05-31
> 新增内容：Mem0 生态分析；Claude Code & OpenCode 基座集成分析 & 方案修订

---

## 一、总览：为什么记忆系统是 AI 工程师的核心

AI 编程智能体本质上是一个**有状态的操作系统**，而非无状态的 LLM 调用。记忆系统是其区别于普通聊天机器人的核心能力：

| 无记忆的 LLM | 有记忆的 AI 工程师 |
|---|---|
| 每次会话从零开始 | 记住用户偏好和项目约定 |
| 重复犯同样的错误 | 从历史中学习，避免重复错误 |
| 无法感知上下文连续性 | 跨会话维护项目心智模型 |
| 每次都是"新手" | 越用越强，积累领域经验 |

以下逐一分析三个代表性系统的设计，然后提出综合方案。

---

## 二、Devin AI（Cognition）

### 2.1 整体架构：感知→规划→记忆→行动的闭环

Devin 运行在一个持续闭环中（类似强化学习的 observation-action-feedback）：

```
while not task_finished:
    perception = env.observe()        # 收集环境状态
    plan = llm.plan(perception)        # 生成下一步计划
    action = executor.run(plan)        # 执行命令或编辑文件
    feedback = env.evaluate(action)    # 检查输出/捕获错误
    memory.store(plan, action, feedback)  # 记录经验
```

### 2.2 三层记忆架构

| 层级 | 名称 | 存储内容 | 持久性 | 类比 |
|---|---|---|---|---|
| L1 | **Working Memory**（工作记忆） | 当前任务上下文：AST、依赖图、打开的文件、对话历史 | 会话级，任务完成后清除 | 开发者正在思考的代码 |
| L2 | **Episodic Memory**（情景记忆） | 过往项目、历史任务、具体的问题解决模式 | 跨会话持久化 | "上周我修过一个类似的 bug" |
| L3 | **Semantic Memory**（语义记忆） | 通用编程范式、最佳实践、框架知识 | 最持久，可跨项目 | 资深工程师的积累的经验 |

### 2.3 关键设计考量

1. **从经验中学习**：情景记忆可以"固化"为语义记忆——将具体的解决问题经验抽象为通用的最佳实践。这是 Devin 区别于"大号自动补全"的关键设计。

2. **多智能体共享记忆**：Devin 的设计影响了后续框架（OpenDevin、AgentVerse、LangGraph），这些框架将记忆从单智能体组件扩展为多智能体共享语义存储。

3. **记忆感知调度**：Planner 模块在分解任务之前先查询记忆，以复用之前的计划和方案。

### 2.4 局限与不确定点

- Cognition 未公开内部技术细节，上述架构主要来自第三方分析和逆向工程
- 工作记忆如何与 LLM 上下文窗口交互、具体的上下文压缩策略不透明
- 情景记忆到语义记忆的固化机制未见可复现的实现方案

---

## 三、OpenCLAW

### 3.1 核心哲学："Text > Brain"——文件优先

OpenCLAW 最鲜明的设计原则是：**所有记忆必须可追溯至磁盘文件**。拒绝将关键信息锁在黑盒向量数据库中。这一选择基于三点考量：

1. **完全透明**——记忆数据是 Markdown 文件，可用任何文本编辑器查看/编辑
2. **低维护成本**——文件系统天然支持版本控制，Git 集成无缝
3. **混合搜索性能**——SQLite 索引文件元数据，搭配 BM25 关键词搜索和向量相似度搜索

### 3.2 三层文件架构

```
~/.openclaw/workspace/
├── atoms/              # 原子存储层：每个记忆片段一个 Markdown 文件
│   └── 3f8e1a2c-20230415-143022.md
├── collections/        # 语义聚合层：通过软链接组织主题分组
│   └── cli_preferences/ → ../atoms/3f8e1a2c-...
└── memory_index.db     # 索引加速层：SQLite 三表结构
    ├── documents       # 文件路径、修改时间、大小
    ├── terms           # 分词后的关键词及位置
    └── vectors         # 向量表示（可选，多模型嵌入）
```

每条原子记忆文件的元数据头：

```yaml
---
type: user_preference
source: chat_session_20230415
timestamp: 1681569022
tags: [bash, cli_tools]
---
用户偏好记录：更喜欢使用 Bash CLI 工具而非 GUI…
```

### 3.3 记忆生命周期（渐进式压缩）

| 阶段 | 时间窗口 | 存储形式 |
|---|---|---|
| 短期记忆 | 最近 7 天 | 完整原子文件 |
| 中期归档 | 30 天以上 | 合并为每日摘要文件 |
| 长期冷存储 | 90 天以上 | 压缩归档至对象存储 |

### 3.4 搜索：混合 BM25 + 向量检索

- **BM25 关键词搜索**：精确查询（如"显示上周的会议记录"）
- **向量余弦相似度**：语义模糊查询（如"找和这个技术方案类似的文档"）
- 在 10 万文档规模下，比纯向量搜索快 3.2 倍，准确率提升 17%

### 3.5 Active Memory（主动记忆系统）

OpenCLAW 的 Active Memory 作为一个独立的子智能体运行在主智能体推理循环**之前**：

1. 每次用户交互触发语义分析
2. 系统从整个记忆存储中提取所有潜在相关的记忆
3. 记忆按优先级排序后注入到提示词顶部
4. 智能体在拥有完整上下文的情况下开始推理

### 3.6 ClawMem——第三方扩展生态

ClawMem 是 OpenCLAW 生态中最成熟的第三方记忆层，融合了多个学术系统的研究成果：

| 组件 | 来源 | 功能 |
|---|---|---|
| QMD | 多信号检索 | BM25 + 向量 + 互惠排名融合 + 查询扩展 + 交叉编码器重排序 |
| SAME | 复合评分 | 时效衰减、置信度、内容类型半衰期、共激活强化 |
| MAGMA | 意图分类 | 多图遍历（语义图、时序图、因果图束搜索） |
| A-MEM | 自演化笔记 | 关键词、标签、因果链接；自动合并和演化 |
| Engram | 模式提取 | 去重 + 频率持续度评分 |

通过 OpenCLAW 的插件钩子总线实现完整生命周期覆盖：

| 钩子 | 触发时机 |
|---|---|
| `before_prompt_build` | 上下文注入 + 会话引导 + 压缩前提取 |
| `agent_end` | 决策提取、交接生成、反馈循环 |
| `before_compaction` | 防御性状态捕获（兜底） |
| `session_start` | 会话注册 + 缓存首轮引导 |

### 3.7 OpenCLAW 设计权衡总结

**优势**：
- 完全透明，可审计，可 Git 追踪
- 插件生态丰富，可灵活扩展
- 主动记忆预热减少冷启动问题

**劣势**：
- 文件系统 I/O 在超大规模下可能成为瓶颈
- 原生缺乏跨会话的自演化能力（依赖第三方插件）
- 缺乏硬性容量约束，可能导致记忆膨胀

---

## 四、Hermes Agent（Nous Research）

### 4.1 核心哲学：工程化约束驱动的认知架构

Hermes 的设计灵感来自**认知科学和 CPU 缓存层级**。其核心理念是：**存储限制不是 bug，而是特性**——强制智能体进行信息策展，防止上下文污染。

### 4.2 五层记忆层级

```


Layer 5: Theory of Mind (Honcho)     ← 心理建模，无限容量
       ↓
Layer 4: Procedural Memory (Skills)  ← ~/.hermes/skills/*.md，可复用工作流
       ↓
Layer 3: Semantic Memory             ← MEMORY.md (~2200 chars) + USER.md (~1375 chars)
       ↓
Layer 2: Episodic Memory             ← SQLite + FTS5，完整历史，按需检索
       ↓
Layer 1: Working Memory              ← LLM Context Window，当前会话
```

**数据向上流动**（原始→精炼→可执行→心理模型），**查询向下流动**（最高置信度层级优先）。

### 4.3 两个核心文件

#### MEMORY.md（~2200 字符 / ~800 tokens）
智能体的"笔记本"：环境事实、项目约定、经验教训。

#### USER.md（~1375 字符 / ~500 tokens）
智能体对用户的理解：偏好、沟通风格、技能水平、行为模式。

**关键设计**：这两个文件不支持 `read` 操作——因为它们始终在会话开始时被注入到 system prompt 中，智能体随时可见。

### 4.4 冻结快照模式（Frozen Snapshot）

这是 Hermes 最精巧的工程决策之一：

```
会话开始 → 加载 MEMORY.md 和 USER.md 的冻结快照到 system prompt
会话期间 → 新信息写入磁盘，但 system prompt 不变
下个会话 → 冻结快照解冻，加载最新版本
```

**为什么这样做？** 因为在会话中更新 system prompt 会使**前缀缓存**失效，大幅增加 API 成本。Hermes Claude Code 通过此模式达到约 92% 的缓存命中率。

### 4.5 Nudge Engine（轻推引擎）

每约 10 轮用户交互触发一次，提示智能体进行自我反思：

- 从任务执行中提炼知识到记忆
- 将重复模式提取为技能文件
- 后台审查，不打断用户

### 4.6 技能系统（程序性记忆）

| 特征 | 说明 |
|---|---|
| 存储格式 | Markdown 文件，带版本控制 |
| 提取触发 | 检测到重复任务（如每周生成报告）→ 自动提炼为标准化技能模板 |
| 优化方式 | 失败 → 触发演化 → 生成变异变体 → A/B 测试选择最优 |
| 安全性 | 安全扫描 + 自动回滚，拒绝恶意写入 |
| 效果 | 后续执行效率提升 60% 以上 |

### 4.7 三层上下文压缩

当接近上下文限制时：

1. **保存**值得保留的信息到记忆
2. **保护**头 3 条 + 尾 4 条消息，压缩中间内容
3. **触发**会话分割（如需要）

### 4.8 Hermes 设计权衡总结

**优势**：
- 硬容量约束防止记忆膨胀和上下文污染
- 冻结快照保护前缀缓存，经济高效
- 完整的学习闭环：执行→评估→提取→优化
- 安全的技能变异机制

**劣势**：
- 严格的字符限制可能丢失边缘重要信息
- 技能系统需要多次重复才能触发，对小项目可能不够及时
- 五层架构增加系统复杂度

---

## 五、Mem0：作为独立记忆层的生态视角

### 5.1 Mem0 的定位差异

与前三个系统不同，Mem0 **不是一个 AI 工程师智能体**，而是一个**独立的通用记忆层（Memory Layer）**，可以插入到任何智能体框架中。这使其视角与前三者有本质区别：

| 维度 | Devin / OpenCLAW / Hermes | Mem0 |
|---|---|---|
| 角色 | 完整的智能体系统 | 记忆基础组件 |
| 记忆 vs 整体 | 记忆是架构的一部分 | 记忆就是全部产品 |
| 集成方式 | 内置 / 插件 | 通过 API / MCP / Provider 接口 |
| 优化目标 | 智能体任务完成度 | 记忆存取效率 + 准确性 |
| 盈利模式 | 产品订阅 | 记忆即服务（MaaS） |

### 5.2 2026 年核心架构：单次 ADD-only 提取 + 多信号检索

Mem0 在 2026 年 4 月发布了全新的 token-efficient 算法，核心四个变化：

#### 1. 单次 ADD-only 提取（Single-pass ADD-only）
- 每次新轮次只做一次 LLM 调用
- 没有 UPDATE 或 DELETE 操作
- 记忆**只增不删**，靠检索时的评分排序来优胜劣汰
- 与 Hermes 的"硬约束策展"哲学完全相反——Mem0 认为写的时候不要判断，读的时候再筛选

#### 2. 智能体生成的事实提升为一等公民
- 以前只存用户说的事实
- 现在智能体的确认、推荐、行动结论也同等权重存储
- 这对 AI 工程师场景尤其关键——智能体在调试中自己发现的结论应该被记住

#### 3. 实体链接（Entity Linking）
- 在 `add()` 时提取实体，建立并行索引
- 检索时匹配查询实体，提升相关记忆排名
- 替代了独立的图数据库，但**牺牲了图遍历能力**

#### 4. 多信号检索融合（Multi-signal Retrieval）
- 三个评分通道并行执行后融合：
  - **语义相似度**（向量嵌入余弦相似度）
  - **关键词匹配**（BM25）
  - **实体匹配**（Entity linking）
- 融合策略：互惠排名融合（Reciprocal Rank Fusion）

### 5.3 2026 年 Benchmark 结果

| 基准测试 | 得分 | 平均 Tokens/查询 | P50 延迟 |
|---|---|---|---|
| **LoCoMo**（1540 题，4 类） | **92.5** | ~6,956 | 0.88s |
| **LongMemEval**（500 题，6 类） | **94.4** | ~6,787 | 1.09s |
| **BEAM (1M)** | **64.1** | ~6,719 | 1.00s |
| **BEAM (10M)** | **48.6** | ~6,914 | 1.05s |

**关键对比**：全上下文基线（Full-context baseline）每次查询消耗 **~26,000 tokens**，Mem0 只用 **~6,900**——**4 倍 Token 节省**，同时保持竞争性准确率。最大提升来自时序推理（+29.6 分）和多跳推理（+23.1 分）。

### 5.4 记忆范围模型（Multi-Scope）

每条记忆写入关联至少一个作用域：

| 作用域 | 语义 | 典型场景 |
|---|---|---|
| `user_id` | 跨所有会话持久化 | 记住用户偏好 |
| `agent_id` | 特定智能体实例 | 记住某个 agent 的专属知识 |
| `run_id` / `session_id` | 单次会话 | 当前对话上下文 |
| `app_id` / `org_id` | 组织级共享 | 团队共享的约定 |

这些作用域在检索时组合使用，结合元数据过滤实现精确的权限隔离。

### 5.5 主动记忆模式（Proactive Memory）

Mem0 提出了三种主动记忆注入模式：

1. **会话开始扫描（Session-Start Scan）**：在会话打开时预取相关记忆，让智能体"到岗即知情"
2. **上下文触发扫描（Context-Trigger Scan）**：监控文件引用、错误信号、话题偏移 → 自动检索相关记忆
3. **定时反思扫描（Scheduled Reflection Scan）**：后台工作者在会话后反思存储的记忆，预计算下次需要什么

这与 OpenCLAW 的 Active Memory 类似，但更强调**触发条件多样性**（不只在会话前，还在会话中关键节点）。

### 5.6 与 Hermes 的集成实践

Hermes v0.7.0 将 Mem0 作为 8 个可插拔记忆提供者之一：

```
推理前：缓存的 Mem0 结果注入 system prompt（零延迟）
推理后：（用户消息, 回复）异步发送到 Mem0 自动提取
轮次间：后台预取下轮需要的内容
```

提供 3 个工具给 LLM：
- `mem0_profile` — 用户完整记忆画像
- `mem0_search` — 带重排序的语义搜索
- `mem0_conclude` — 逐字事实存储（不做提取）

断路器机制：连续 5 次失败 → 暂停 2 分钟，智能体继续工作不受影响。

### 5.7 开放问题

Mem0 的 2026 年报告指出了行业仍未解决的难题：

| 问题 | 本质 |
|---|---|
| **时序抽象（Temporal abstraction）** | BEAM 1M→10M 分数下降 ~25%，时序查询是最难的类别。规模扩大 10 倍，性能明显下降 |
| **跨会话结构演化** | 用户搬家应该被视为演化而非替换——但大多数系统把变化当替换处理 |
| **应用层评估** | 通用 benchmark 无法预测医疗/法律等垂直领域的实际表现 |
| **记忆过时（Staleness）** | 高频检索的事实（如"用户在 Google 工作"）在用户换工作后变得自信地错误——低分记忆有时效衰减，高分记忆的过时是更难的问题 |
| **跨会话身份解析** | 匿名会话、多设备用户、混合认证流都假设稳定的 user_id，现实不总是如此 |

### 5.8 Mem0 设计权衡总结

**优势**：
- Token 效率极高（全上下文的 ~25%）
- 集成生态丰富（21 框架 + 20 向量存储）
- 多作用域隔离适合多租户
- 主动记忆模式减少冷启动

**劣势**：
- ADD-only 策略导致存储膨胀（靠检索筛选而非写入时约束）
- 实体链接牺牲了图遍历能力（不能做多跳关系查询）
- 云服务依赖（自部署复杂度较高）
- 不是完整记忆系统——不管理记忆生命周期（无自动压缩/清理）
- Benchmark 争议（第三方的复现评分低于自报评分）

---

## 六、五家方案横向对比

| 维度 | Devin AI | OpenCLAW | Hermes Agent |
|---|---|---|---|
| **层级数** | 3 层（工作/情景/语义） | 3 层（原子/集合/索引） + 生命周期 | 5 层（工作/情景/语义/程序/心智） |
| **存储介质** | 未公开（推测为内部数据库） | Markdown 文件 + SQLite | Markdown 文件 + SQLite/FTS5 |
| **搜索方式** | 隐式（记忆查询内嵌于规划循环） | BM25 + 向量混合检索 | FTS5 全文搜索 + System Prompt 注入 |
| **上下文注入时机** | 感知阶段内嵌 | Active Memory 子智能体（主循环前） | 会话开始冻结快照 |
| **记忆容量约束** | 未公开 | 无硬性约束（靠生命周期管理） | 严格硬限制（2200/1375 chars） |
| **自演化能力** | 情景→语义固化（未公开） | 通过 ClawMem 插件（A-MEM） | Nudge + 技能变异 + 轨迹 RL |
| **多智能体共享** | 支持（共享语义存储） | 通过插件支持 | 通过 Honcho 支持 |
| **前缀缓存感知** | 未公开 | 否 | 是（冻结快照模式） |
| **安全机制** | 未公开 | SQLite 加密 + AES-256 | 技能自动回滚 |
| **项目范围感知** | 隐式（通过 AST/依赖图） | 通过 collections 组织 | 通过 CWD 过滤（提案中） |
| **开源** | 否 | 是（MIT） | 是（开源） |

### 核心设计哲学差异

| | Devin | OpenCLAWHermes |
|---|---|---|
设计隐喻 | 一个成长中的工程师 | 文件系统 + 搜索引擎 | 认知架构 + CPU 缓存 |
| 首要目标 | 智能体能力最大化 | 透明度/可审计性最大化 | 成本效益比最优化 |
| 对 LLM 的态度 | 作为大脑使用 | 作为处理器使用 | 作为资源进行管理 |
| 记忆的本质 | 经验积累 | 文件索引 | 约束下的信息策展 |

---

## 六、我的设计方案

### 6.1 设计原则

基于上述调研，我认为一个理想的 AI 工程师记忆系统应遵循以下原则：

#### 原则 1：文件即真相（File as Ground Truth）——来自 OpenCLAW
- 所有记忆最终以 Markdown 文件存储，可 Git 追踪、可人类阅读、无厂商锁定
- SQLite 仅作为加速索引，不是主存储
- 问："如果我删除数据库，会丢失记忆吗？" 答案应该是"不会，重建索引即可"

#### 原则 2：硬约束驱动策展（Hard Constraints Drive Curation）——来自 Hermes
- 注入到 System Prompt 的记忆必须有严格的容量上限
- 容量限制迫使智能体优先存储高价值信息
- 无限制的记忆= 噪音，噪音 = token 浪费 = 质量下降

#### 原则 3：层级化存储与访问（Layered Storage & Access）——综合三家
- 不同层级的记忆有不同的存储介质、访问频率和检索方式
- 热数据在 prompt 中 → 温数据在 SQLite 中 → 冷数据在文件系统中
- 每层都有明确的容量/延迟/成本特征

#### 原则 4：前缀缓存感知（Cache-Aware Design）——来自 Hermes
- System prompt 在会话期间不可变，保护前缀缓存
- 记忆写入实时完成，但读取在下一会话生效
- 这是 API 经济性的关键

#### 原则 5：主动演化（Active Evolution）——综合三家
- 从情景记忆中自动提取事实 → 语义记忆
- 从重复模式中自动提取技能 → 程序性记忆
- 检测矛盾并解决/废弃旧记忆

### 6.2 四层记忆架构

```
┌─────────────────────────────────────────────────────────────┐
│                   L1: 工作记忆 (Working Memory)               │
│   LLM Context Window / 当前会话状态                          │
│   容量: 动态（上下文窗口减去其他开销）                         │
│   持久性: 会话级，自动压缩/清理                              │
│   存储: 内存                                                  │
├─────────────────────────────────────────────────────────────┤
│                   L2: 情景记忆 (Episodic Memory)              │
│   原始对话历史 / 工具调用轨迹 / 错误模式                     │
│   容量: 无限（受磁盘限制）                                    │
│   持久性: 跨会话，有时效衰减                                 │
│   存储: SQLite + FTS5 + 向量索引                              │
│   访问: 按需搜索工具（search_episodic）                      │
├─────────────────────────────────────────────────────────────┤
│                   L3: 语义记忆 (Semantic Memory)              │
│   项目约定 / 用户偏好 / 经验教训 / 架构决策                 │
│   容量: 严格限制（~4K chars MEMORY.md + ~2K chars USER.md） │
│   持久性: 跨会话，持续更新                                   │
│   存储: Markdown 文件（始终注入 system prompt）              │
│   访问: add / replace / remove 操作                          │
├─────────────────────────────────────────────────────────────┤
│                   L4: 程序性记忆 (Procedural Memory)          │
│   可复用工作流 / 技能模板 / 自动化脚本                      │
│   容量: 按技能文件数限制（~50个技能）                        │
│   持久性: 跨会话，版本控制                                   │
│   存储: Markdown 文件 + Git 版本管理                          │
│   访问: 技能名称调用 + 参数注入                              │
└─────────────────────────────────────────────────────────────┘
```

### 6.3 各层详细设计

#### L1: 工作记忆

**存储内容**：
- 当前任务描述和目标
- 已打开的文件的摘要（路径、关键符号、最近修改行）
- 最近 N 步工具调用轨迹
- 当前问题的中间状态（如部分完成的重构）

**上下文预算管理**：

| 用途 | 分配比例 | 说明 |
|---|---|---|
| 任务上下文 | 40% | 当前问题描述、相关代码片段 |
| 语义记忆 | 20% | MEMORY.md + USER.md 注入 |
| 情景检索结果 | 20% | 按需搜索的历史相关片段 |
| 输出空间 | 20% | 保证模型有足够的生成空间 |

**压缩策略**（接近上下文限制时）：
1. 保存关键学到的东西到 L3
2. 保留头 3 条 + 尾 5 条消息，中间生成摘要
3. 如果仍然超限，触发会话分割信号

#### L2: 情景记忆

**存储结构**：

```sql
-- SQLite 核心表
CREATE TABLE episodes (
    id TEXT PRIMARY KEY,          -- UUID
    session_id TEXT,              -- 所属会话
    timestamp INTEGER,            -- Unix 时间戳
    turn_index INTEGER,           -- 会话内轮次
    role TEXT,                    -- user / assistant / tool
    content TEXT,                 -- 完整内容
    content_type TEXT,            -- conversation / code_block / error / tool_call
    project TEXT,                 -- 项目路径哈希（用于过滤）
    token_count INTEGER           -- token 数估计
);

CREATE TABLE episode_vectors (
    episode_id TEXT PRIMARY KEY,
    vector BLOB,                  -- 嵌入向量
    model TEXT                    -- 嵌入模型名称
);

-- FTS5 全文搜索
CREATE VIRTUAL TABLE episode_fts USING fts5(
    content, content=episodes
);
```

**时效衰减**：每条记忆的 relevance score 随年龄指数衰减：

```
score = base_score * exp(-λ * days_since_creation)
```

其中 λ 可配置（默认 0.05，约 14 天半衰期）。

**自动清理**：超过 90 天的低分记忆自动归档到压缩文件，保留 FTS 索引但删除向量。

#### L3: 语义记忆（核心创新）

**文件结构**：

```
.project/memory/
├── MEMORY.md          # 项目级事实（~4K chars）
├── USER.md            # 用户级偏好（~2K chars）
└── GLOBAL.md          # 全局知识（~2K chars，多项目共享）
```

**MEMORY.md 内容优先级**（按重要性降序）：

```
[最高] 用户明确纠正 → 用户偏好 → 项目约定 → 架构决策 
      → 环境配置 → 错误教训 → 工作流笔记 [最低]
```

**写入失败即反馈**：当超出容量时，`add` 操作返回错误 + 当前所有条目，引导智能体使用 `replace` 或 `remove`：

```json
{
  "success": false,
  "error": "Memory at 4,123/4,000 chars. Replace or remove existing entries first.",
  "current_entries": [...]
}
```

**项目范围过滤**：根据当前工作目录自动过滤，只注入相关项目的记忆，避免跨项目噪音。

#### L4: 程序性记忆（技能系统）

**技能文件结构** (`~/.ai-engineer/skills/<skill-name>.md`)：

```yaml
---
name: db_migration
version: 3
triggers: ["migrate database", "schema change", "alter table"]
confidence: 0.85
created: 2026-03-15
updated: 2026-05-28
history: [
  {version: 1, outcome: "success", timestamp: "2026-03-15"},
  {version: 2, outcome: "failure", timestamp: "2026-04-20"},
  {version: 3, outcome: "success", timestamp: "2026-05-28"}
]
---

## 目标
安全执行数据库迁移，支持回滚

## 步骤
1. 备份当前数据库
2. 生成迁移脚本（prisma migrate dev / alembic 等）
3. 在事务中执行迁移
4. 验证 schema 与原数据完整性
5. 如失败，执行回滚流程

## 参数
- `db_type`: postgres | mysql | sqlite
- `migration_tool`: prisma | alembic | flyway

## 注意事项
- 生产环境必须先执行 `--dry-run`
- 大表加列需要评估锁表时间
```

**技能提取触发条件**：
- 同一模式的任务序列重复 ≥ 3 次
- 单次任务包含 ≥ 5 个工具调用且有明确可复用的结构
- 用户主动要求"记住这个流程"

**技能优化**：
- 执行失败 → 收集错误 → 生成变异 → A/B 测试
- 成功路径强化权重，失败路径降权或废弃
- 自动检测技能间的重复与冲突

### 6.4 数据流

```
用户输入
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                   推理前阶段 (Pre-Reasoning)                  │
│                                                             │
│  1. 加载冻结快照（MEMORY.md + USER.md + GLOBAL.md）         │
│     → 注入到 System Prompt（利用前缀缓存）                   │
│                                                             │
│  2. Active Memory 检索（并行）                               │
│     → 从 L2 情景记忆中检索相关片段                          │
│     → 评分、去重、排序，注入到上下文                         │
│                                                             │
│  3. 项目上下文发现                                          │
│     → 检测 CWD 下的 AGENTS.md / CLAUDE.md                   │
│     → 注入项目级配置                                        │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                     推理阶段 (Reasoning)                     │
│                                                             │
│  LLM 在完整上下文中生成回复/行动                             │
│  可主动调用：                                                │
│    - memory_search（搜索 L2 情景记忆）                       │
│    - memory_add / replace / remove（更新 L3 语义记忆）      │
│    - skill_execute（调用 L4 技能）                          │
│    - skill_create / update（创建或更新技能）                │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│                     推理后阶段 (Post-Reasoning)               │
│                                                             │
│  1. Nudge 检查（每 ~8 轮触发）                              │
│     → 是否有值得保存到 L3 的新信息？                        │
│     → 是否有重复模式可以提取为技能？                        │
│     → 是否有矛盾需要解决？                                  │
│                                                             │
│  2. 情景记录                                                │
│     → 本轮交互写入 L2（异步，不阻塞）                       │
│                                                             │
│  3. 上下文预算检查                                          │
│     → 是否接近限制？执行压缩策略                            │
└─────────────────────────────────────────────────────────────┘
```

### 6.5 关键创新点

#### 1. 分级上下文注入
不是所有记忆都注入到每个请求。引入三级选择性注入：

| 级别 | 注入对象 | 触发条件 |
|---|---|---|
| Always | MEMORY.md + USER.md 冻结快照 | 每次请求，利用前缀缓存 |
| On-demand | L2 情景检索结果 | 仅在模型主动调用 search 时 |
| Background | 项目配置文件 | 仅在项目切换或首次进入时 |

#### 2. 主动记忆固话（Consolidation）
后台进程（异步、低优先级）：

```
情景记忆 (L2)                      语义记忆 (L3)
─────────────────                ─────────────────
"用户把 axios 换成了       →     "项目使用 fetch，不使用 axios"
 fetch，并修改了所有                  （来源：观察到 5 次修改记录）
 12 个文件的导入"

"第三次用 prisma migrate   →     "数据库迁移使用 Prisma，包含 dry-run + 事务回滚"
 做 schema 变更了"                   （来源：3 次成功执行轨迹）

"用户说不要用 any 类型"    →     "项目禁用 TypeScript any 类型"
                  （来源：用户明确指示）
```

#### 3. 矛盾检测与解决

```
检测到：
  MEMORY.md: "项目使用 pnpm"
  MEMORY.md: "项目使用 yarn"（来自更早的记录）

解决：
  - 比较两条记录的时间戳
  - 参考相关情景记录确认最近的实际操作
  - 保留较新的一条，废弃较旧的一条
  - 记录废弃原因
```

#### 4. 前缀缓存预热

```
会话 A: system prompt = [身份层 + MEMORY.md 快照 + USER.md 快照 + 任务层]
                                                           ↑
                                                    前缀缓存命中
会话 B（同一项目）: system prompt = [身份层 + MEMORY.md 快照 + USER.md 快照 + 新任务]
                                  ↑
                           缓存命中，省 ~60% token 开销
```

### 6.6 与各系统的对比

| 维度 | Devin AI | OpenCLAW | Hermes | Mem0 | **本方案** |
|---|---|---|---|---|---|
| 本质定位 | 智能体系统 | 智能体系统 | 智能体系统 | **记忆层组件** | 智能体系统 |
| 层级 | 3 | 3 | 5 | 2（提取+检索） | **4** |
| 存储介质 | 未公开 | Markdown + SQLite | Markdown + SQLite | 向量存储（可插拔） | **Markdown + SQLite + 向量** |
| 存储透明度 | 低 | 高 | 高 | 中（向量不可读） | **高** |
| 容量约束 | 未公开 | 无硬约束 | 严格 | ADD-only（无约束） | **严格 + 分级** |
| 前缀缓存感知 | 未知 | 否 | **是（冻结快照）** | 否（依赖集成方） | **是 + 预热机制** |
| 搜索方式 | 隐式 | BM25 + 向量 | FTS5 | **语义 + BM25 + 实体（融合）** | **混合检索** |
| Token 效率 | 未公开 | 低（~20-30%预算被记忆占用） | 中（限制容量） | **高（~6.9K/查询）** | **高（4层分级注入）** |
| 技能系统 | 未公开 | 通过插件 | **内置** | 无 | **内置 + 自动提取** |
| 主动固话 | 有（未公开） | 通过 ClawMem | Nudge 驱动 | ADD-only（不固话） | **后台异步固话 + 矛盾检测** |
| 项目隔离 | 隐式 | 通过集合 | 提案中 | 通过作用域过滤 | **CWD 自动识别** |
| 集成生态 | 无 | 插件生态 | 8+ 提供者 | **21框架 + 20存储** | 插件接口 |
| 安全性 | 未公开 | 加密 | 自动回滚 | API Key 鉴权 | **自动回滚 + 加密** |
| 复杂度 | 高 | 中 | 中高 | 低（组件级别） | **中** |

### 6.7 实现路线图

| 阶段 | 内容 | 可独立交付？ |
|---|---|---|
| **Phase 1** | L2 情景记忆（SQLite + FTS5 + 搜索工具） | ✅ 是 |
| **Phase 2** | L3 语义记忆（MEMORY.md/USER.md + 大小约束 + 冻结快照） | ✅ 是 |
| **Phase 3** | Active Memory 检索（推理前自动情景检索 + 评分排序） | 依赖 Phase 1, 2 |
| **Phase 4** | L4 程序性记忆（技能系统 + 版本控制 + 执行框架） | ✅ 是 |
| **Phase 5** | 主动固话 + 矛盾检测（后台异步处理） | 依赖 Phase 1-3 |
| **Phase 6** | 技能自动提取 + 变异优化 | 依赖 Phase 4, 5 |
| **Phase 7** | 多项目隔离 + GLOBAL.md | 依赖 Phase 2 |

---

## 七、结论

### 7.1 四家的核心启示

| 系统 | 最值得学习的点 | 最应避免的点 |
|---|---|---|
| **Devin AI** | 记忆驱动能力持续增长的架构理念；闭环学习愿景 | 封闭系统、设计细节不可复现 |
| **OpenCLAW** | 文件即真相的设计哲学、Active Memory 预热机制 | 缺乏硬约束导致的记忆膨胀和 Token 浪费 |
| **Hermes** | 约束策展、冻结快照、Nudge 驱动的技能演化闭环 | 容量限制过于苛刻可能丢失边缘信息；缺乏向量检索 |
| **Mem0** | Token 效率极高的多信号检索；丰富的集成生态；主动记忆的三种触发模式 | ADD-only 导致存储膨胀；Dependency 过重（需外部存储）；Benchmark 争议 |

### 7.2 Mem0 视角对本方案的修正

研究 Mem0 后，我对原方案做了以下调整：

#### 修正 1：L2 情景记忆加入多信号检索
原方案仅使用 FTS5 全文搜索，现在加入向量嵌入 + 实体链接，形成三路并行检索（语义 + BM25 + 实体匹配），参考 Mem0 的融合策略。

#### 修正 2：引入"写时不判断，读时再筛选"思想
原方案偏 Hermes（写入时就严格策展），Mem0 的 ADD-only 思路提供了一个有用的补充：对于 L2 情景记忆，**不做写入过滤，靠检索评分优胜劣汰**。L3 语义记忆仍保持 Hermes 风格的硬约束。两层不同策略：

| 层级 | 写入策略 | 检索策略 | 哲学来源 |
|---|---|---|---|
| L2 情景记忆 | **ADD-only**（不筛选） | 多信号评分 + 排序 | Mem0 |
| L3 语义记忆 | **严格容量约束** | 始终注入 system prompt | Hermes |
| L4 程序性记忆 | **模式识别触发** | 按名称调用 | Hermes + 自研 |

#### 修正 3：加入三种主动记忆触发模式
原方案只有"会话开始预取"一种模式。现在参考 Mem0 的三模式：

| 触发时机 | 行为 | 实现复杂度 |
|---|---|---|
| **会话开始** | 预取项目相关记忆 | 低 |
| **上下文触发** | 文件引用、错误信号、话题偏移时自动检索 | 中 |
| **定时反思** | 每 N 轮后后台扫描并预计算 | 中高 |

#### 修正 4：加入"记忆过时"检测
原方案有矛盾检测但未考虑"自信地错误"问题。新增机制：**对高频检索的记忆条目，如果长时间未被用户行为确认，自动降权**。

### 7.3 本方案的最终核心主张

1. **记忆不是存储问题，是策展问题**——重要的不是记住多少，而是记住什么
2. **写时宽松，读时严格**——L2 不筛选全量存，L3 硬约束精炼存，各司其职
3. **文件是基础，索引是加速，prompt 是战场**——三层各司其职
4. **API 成本是硬约束，设计必须感知**——前缀缓存不是优化选项，而是架构前提
5. **最好的记忆系统是"越用越少"的系统**——技能提取将序列化的操作步骤转化为声明式知识，减少每一步的决策负担
6. **自我演化不是增值功能，是必要条件**——没有演化能力，记忆系统就是一个静态数据库而非智能体的大脑

### 7.4 一句话总结

> **理想的 AI 工程师记忆系统 = OpenCLAW 的透明文件哲学 + Hermes 的约束策展与缓存感知 + Devin 的持续演化愿景 + Mem0 的 Token 高效多信号检索，再加上分层写策略（L2 ADD-only / L3 硬约束）和三种主动记忆触发模式。**

---

## 八、基座集成分析：以 Claude Code / OpenCode 为底座

> **核心问题**：我们的 AI 工程师运行在 ACP Agent（如 Claude Code、OpenCode）之上，它们已经有了各自的记忆系统——是融合进去，还是自己做？

---

### 8.1 Claude Code 的现有记忆系统全景

Claude Code 是目前功能最完整的 AI 编程智能体之一，其记忆系统在 2026 年 3 月源码泄露后已全面公开。

#### 四层架构

| 层级 | 名称 | 写入方式 | 存储位置 | 容量限制 | 检索方式 |
|---|---|---|---|---|---|
| **L0** | CLAUDE.md | **手动编写** | 项目目录 | 建议 ~200 行 | 每次会话加载到上下文 |
| **L1** | Auto Memory | **AI 自动写入** | `~/.claude/projects/<hash>/memory/` | MEMORY.md 索引 **200 行 / 25KB** | Sonnet Side Query（非向量）选 5 个 topic 文件 |
| **L2** | Auto Dream | **后台 Fork Agent 固话** | 同上 | 同 L1，需 ≥24h + ≥5 新会话触发 | 定时后台梳理 |
| **L3** | KAIROS | **始终在线守护进程**（未发布） | 代码日志 + Memory | 时间约束（>15s 操作延迟） | 定时 `<tick>` 信号 |

#### Auto Memory 的四个记忆类型（封闭分类）

| 类型 | 可见性默认值 | 举例 |
|---|---|---|
| **user** | 始终私有 | 用户角色、专业领域、沟通偏好 |
| **feedback** | 默认私有；项目级约定可团队共享 | 被纠正过的模式、验证过的做法 |
| **project** | 偏向团队共享 | 活动工作、目标、截止日期、非代码可推导的决策 |
| **reference** | 通常团队共享 | 外部系统的指针（"Bug 在 Linear 的 INGEST 项目追踪"） |

**关键规则**：禁止存储代码当前状态可推导的信息（代码模式、Git 历史、架构）——防止记忆与现实产生漂移。

#### Auto Dream 的工作机制

```
触发条件: ≥24h 无清理 AND ≥5 个新会话累积
                                
Fork Agent（只读 bash + 限于 memory 目录的编辑权限）
    │
    ├── Phase 1: Orient — 评估当前记忆状态
    ├── Phase 2: Gather — 跨会话收集候选内容
    ├── Phase 3: Consolidate — 合并/重写/消解矛盾
    └── Phase 4: Prune — 剔除不配占用空间的内容
```

#### 关键设计特征

| 特征 | 说明 |
|---|---|
| **前缀缓存** | System Prompt 在 `DYNAMIC_BOUNDARY` 处分割。Auto Memory 在动态段——修改会破缓存 |
| **过期防护** | 超过 1 天的记忆自动标注过期警告："记忆是时间点的观察，不是实时状态" |
| **信任当前状态** | 代码与记忆冲突时，信任代码——更新或删除记忆，而非按记忆行动 |
| **记忆忽略** | 用户说"忽略记忆"→ 系统表现如 MEMORY.md 为空 |
| **团队记忆** | `memory/team/` 目录实现团队共享；300 行安全防护（路径遍历、符号链接、空字节注入） |

#### Claude Code 记忆系统的核心局限

| 局限 | 影响 |
|---|---|
| **纯关键词检索**（无向量/语义） | "port conflicts" 搜不到 "docker-compose port mapping" |
| **每次最多 5 个 topic 文件** | 数百个记忆中每次只可见 5 个 |
| **200 行索引硬上限** | 长期项目旧条目被静默推走 |
| **锁定在 Claude Code 内部** | 无法导出，切换 Agent 则从零开始 |
| **无程序性/技能记忆** | 无法复用多步骤工作流 |
| **不含版本历史** | 误编辑或误删除无法回滚 |

---

### 8.2 OpenCode 的现有记忆生态

OpenCode **没有内置记忆系统**——完全依赖社区插件。

#### 主流方案

| 方案 | 存储 | 检索 | 特点 |
|---|---|---|---|
| **opencode-mem** | SQLite + USearch 向量索引 | 向量搜索 | 最广泛使用；有 Web UI；自动捕获 + 去重 |
| **Muninn** | ChromaDB | MCP 工具 + 符号索引 | 支持符号索引（函数/类/方法）；跨项目全局记忆 |
| **opencode-agent-memory** | Markdown 文件 + YAML frontmatter | System prompt 注入 | Letta 风格的自编辑记忆块；简单直接 |
| **Lore** | SQLite FTS5 + 蒸馏管道 | 三级召回 | 三阶蒸馏：原始→时间线→结构化文档；~85% 召回率 |
| **harness-memory** | SQLite WASM | 4 层激活引擎 | 人类审核流程；73% token 节省 |

#### OpenCode 记忆生态的关键特征

- **社区碎片化**：7+ 个互相竞争的方案，无标准
- **MCP 协议**：多数方案通过 MCP 集成，理论上可互换
- **轻量级向量**：USearch、ChromaDB 等本地嵌入为主
- **Lore 的蒸馏架构最成熟**：三阶递进式压缩，是目前召回准确率最高的方案

---

### 8.3 融合 vs 自建：决策分析

#### 决策维度矩阵

| 维度 | 融合进 Claude Code | 融合进 OpenCode | 自建独立层 |
|---|---|---|---|
| **启动成本** | ✅ 低（复用已有记忆系统） | ❌ 高（OpenCode 无内置） | 中 |
| **语义检索** | ❌ Claude Code 不支持向量 | ✅ 插件已支持向量 | ✅ 可原生支持 |
| **技能/程序性记忆** | ❌ 不支持 | ❌ 社区方案也不支持 | ✅ 可原生设计 |
| **跨 Agent 共享** | ❌ 锁定在 Claude Code | ✅ MCP 可共享 | ✅ MCP 可共享 |
| **前缀缓存优化** | ✅ 已内置 | N/A（无内置记忆） | ❌ 需自己实现 |
| **Dream 固话** | ✅ 已内置 | ❌ 需自己实现 | ❌ 需自己实现 |
| **记忆导出/迁移** | ❌ 无法导出 | ✅ 文件可导出 | ✅ 文件可导出 |
| **功能可定制性** | ❌ 受限于黑盒 | ✅ 开源可改 | ✅ 完全可控 |
| **维护成本** | ✅ 低（交给上游） | 中 | ❌ 高 |

#### 各基座的核心结论

**在 Claude Code 之上**：
- Claude Code 的 Auto Memory + Dream 已经覆盖了 **L3 语义记忆**（事实/偏好/反馈）和**后台固话**的需求
- 它的**最大短板是检索**——纯关键词无语义，且 5 文件上限
- **融合策略**：不动它的 Auto Memory，通过 Hooks/MCP **补充向量检索层**来增强 L2 情景记忆
- 目标：Claude Code 管"记住什么"（事实策展），我们管"找到什么"（语义检索）

**在 OpenCode 之上**：
- OpenCode 本身没有记忆系统，**实际上是空白画布**
- **自建策略**：从头构建完整的记忆架构（L1 工作记忆 + L2 情景记忆 + L3 语义记忆 + L4 程序性记忆）
- 但可利用 MCP 协议对接现有的向量存储和嵌入模型

---

### 8.4 推荐的混合方案

#### 总体原则：**"利用基座的能力，填补基座的空白"**

不要追求"要么全融合，要么全自建"。应该按记忆层级拆分决策：

| 层级 | Claude Code 环境 | OpenCode 环境 |
|---|---|---|
| **L1 工作记忆**（当前上下文） | ✅ 复用 Claude Code 内置 | ✅ 复用 OpenCode 上下文窗口 |
| **L2 情景记忆**（历史 + 语义检索） | 🔶 **增强**：Hooks 注入向量检索层 | 🔶 **自建**：用 MCP 对接向量存储 |
| **L3 语义记忆**（事实/偏好/反馈） | ✅ **融合**：用 Claude Code Auto Memory | 🔶 **自建**：Markdown 文件 + 注入 |
| **L4 程序性记忆**（技能/工作流） | 🔶 **新增**：Hooks 注入技能系统 | 🔶 **自建**：Markdown 技能文件 |

#### Claude Code 环境的具体方案

```
Claude Code 原生                               我们新增（Hooks / MCP）
═══════════════                               ════════════════════════

CLAUDE.md (手动)                               L2 情景记忆增强
Auto Memory (自动事实)    ← 不动，融合 →         - 向量检索层 (SQLite-vec/Qdrant)
Auto Dream (后台固话)                           - 实体链接
                                              - 推理前预取 + 上下文触发
                                              
                                              L4 程序性记忆
                                              - Skills Markdown 文件
                                              - 技能提取 + 自动触发
                                              - 版本控制 + 变异优化
```

**融合接口**：
- **写入**：用 Claude Code 的 `hasMemoryWritesSince()` 机制检测是否需手动写入 L3；我们只操作 L2 和 L4
- **读取**：在 `before_prompt_build` hook 中将向量检索结果注入到上下文
- **固话**：Claude Code 的 Auto Dream 管 L3 固话；我们独立做 L2→L4（轨迹→技能）的固话

#### OpenCode 环境的具体方案

```
我们自建（MCP + 文件）                         充分利用的 OpenCode 原生
══════════════════                            ═══════════════════════

L1 工作记忆                                    上下文窗口管理
L2 + L3 + L4 全栈记忆系统                       工具调用能力
  - SQLite + 向量索引                           文件系统访问
  - Markdown 事实文件                           终端执行能力
  - 技能系统
  - Background 固话
```

---

### 8.5 与现有方案的详细对比（含基座考量）

| 维度 | Claude Code 原生 | 我们 + Claude Code（融合） | 我们的独立方案（OpenCode 等） |
|---|---|---|---|
| 语义检索 | ❌ 无 | ✅ 新增向量层 | ✅ 原生 |
| 事实记忆策展 | ✅ Auto Memory | ✅ 复用 | 需自建 |
| 后台固话 | ✅ Auto Dream | ✅ 复用 | 需自建 |
| 程序性记忆 | ❌ 无 | ✅ Hooks 注入 | ✅ 原生 |
| 前缀缓存 | ✅ 已优化 | ✅ 不动原 prompt | 需自建 |
| 跨 Agent 共享 | ❌ 锁定 | ❌ 部分（L2/L4 可共享） | ✅ 完全可共享 |
| 记忆导出 | ❌ 不可导出 | ❌ L3 不可导出 | ✅ 全部可导出 |
| 技能自动提取 | ❌ 无 | ✅ 新增 | ✅ 原生 |
| 矛盾检测 | ✅ Dream 做 | ✅ 复用 | 需自建 |
| 过时检测 | ✅ 过期标注 | ✅ 复用 | 需自建 |
| 集成复杂度 | — | 低（Hooks/MCP 挂载） | 中高（全栈） |

---

### 8.6 实施路线图（基于基座版本）

#### 场景 A：优先服务 Claude Code 用户

| 阶段 | 内容 | 交付物 |
|---|---|---|
| **Phase A1** | L2 向量检索增强 | Claude Code Hooks：`before_prompt_build` 注入语义检索结果 |
| **Phase A2** | L4 技能系统 | Skills Markdown + Hook 注入；自动提取重复模式 |
| **Phase A3** | 上下文触发检索 | 监控文件引用/错误信号→自动检索 L2 |
| **Phase A4** | 技能变异优化 | 失败→变异→A/B 测试闭环 |

**特点**：轻量启动，不侵入 Claude Code 原生记忆，仅做"加法"

#### 场景 B：需要跨 Agent 支持（Claude Code + OpenCode + 其他）

| 阶段 | 内容 | 交付物 |
|---|---|---|
| **Phase B1** | 独立记忆层核心 | MCP 服务器：L2 + L3 基础功能 |
| **Phase B2** | 双基座适配器 | Claude Code Hook 插件 + OpenCode MCP 插件 |
| **Phase B3** | L4 技能系统 | 统一 skill 格式，双基座共享 |
| **Phase B4** | 后台固话 | 独立 Dream 进程（不依赖 Claude Code Auto Dream） |

**特点**：成本更高，但完全独立，不受制于任何基座

---

### 8.7 最终建议

| 判断 | 结论 |
|---|---|
| **要不要融合 Claude Code** | **要，但只融合 L3（语义记忆）**。Claude Code 的 Auto Memory + Dream 在"记住事实"这件事上做得很好，不应重造。 |
| **要不要自己建 L2（情景检索）** | **要**。Claude Code 的纯关键词检索是最大短板，用 Hooks 补上向量层。 |
| **要不要自己建 L4（技能）** | **要**。Claude Code 完全没有程序性记忆，这是最大的未满足需求。 |
| **要不要支持 OpenCode** | **视需求**。如果只服务 Claude Code 用户，Phase A 方案足够。需要跨 Agent 则走 Phase B。 |
| **要不要用 Mem0 作为 L2 后端** | **可以作为选项，但不绑定**。Mem0 的集成生态好，但依赖外部服务。推荐同时支持本地 SQLite-vec 和 Mem0 两种后端。 |

#### 一句话策略

> **融合 Claude Code 的"事实记忆"（L3），自建"语义检索"（L2）和"技能系统"（L4），通过 Hooks/MCP 作为胶水层。对 OpenCode，提供同一套方案的 MCP 版本。两份实现共享相同的核心存储格式和检索算法。**

#### 架构图

```
                     ┌──────────────────────────────────────┐
                     │            你的 AI 工程师              │
                     │         (指令 + 决策逻辑)              │
                     └──────────────────────────────────────┘
                                      │
                    ┌─────────────────┼─────────────────┐
                    │                 │                 │
                    ▼                 ▼                 ▼
          ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
          │  Claude Code    │ │  你自己的层       │ │  OpenCode       │
          │  作为执行引擎    │ │                  │ │  作为执行引擎    │
          │                 │ │ • L2 向量检索     │ │                 │
          │ • L3 事实记忆   │ │ • L4 技能系统     │ │ • L1 工作记忆    │
          │ • L1 工作记忆   │ │ • 上下文触发      │ │ • 工具执行       │
          │ • 工具执行      │ │ • 跨 Agent 共享   │ │                  │
          └─────────────────┘ └─────────────────┘ └─────────────────┘
```

这个架构中，Claude Code 和 OpenCode 都是**执行引擎**（类似操作系统内核），你的记忆系统是**中间件层**（类似数据库），上层是**智能体编排层**。三者职责清晰，不互相侵入。`
